# Irix Project Status

> Current developer handoff note for the Irix Windows PoC.
> Last verified: 2026-06-13.

---

## Canonical Docs

| Doc | Purpose |
|-----|---------|
| [Irix_Framework_Design.md](Irix_Framework_Design.md) | Main architecture design, layer boundaries, ADR index, and private execution scope. |
| [Active-Worklist.md](Active-Worklist.md) | Current private work items and framework-promotion candidates. |
| [Style-System.md](Style-System.md) | Style layer contract: layout style, visual style, text shaping style, composition style, and control-state style. |
| [Animation-Composition.md](Animation-Composition.md) | Animation ownership contract: UI-runtime animation, compositor animation, hybrid animation, and scroll presentation model. |
| [GPU-Composition-Architecture.md](GPU-Composition-Architecture.md) | Future platform-neutral composition/GPU-offload architecture for D3D12 now and Vulkan/Metal later. |
| [D3D12-Composition.md](D3D12-Composition.md) | Active D3D12 composition implementation for transform/opacity and fixed-clip scroll ticks over retained draw output. |
| [Color-Pipeline.md](Color-Pipeline.md) | Canonical Irix color contract: ideal linear BT.2020 value, current SDR/sRGB output stage, and deferred HDR output mapping owner. |
| [Poc-Promotion-Contracts.md](Poc-Promotion-Contracts.md) | Promotion contracts for `Irix.Poc` runtime adapters, Windows backend adapters, and legacy/debug presentation paths. |
| [Glyph-Atlas-Design.md](Glyph-Atlas-Design.md) | D3D12 glyph atlas text renderer design, degradation policy, and guarded coverage expansion. |
| [Glyph-Atlas-Entry-Eviction-Design.md](Glyph-Atlas-Entry-Eviction-Design.md) | Design-only preconditions and non-goals for future entry-level LRU / sub-rect free-list work. |
| [ADR-Scissor-Clipping.md](ADR-Scissor-Clipping.md) | Clip/scissor/text-clip decision and guarded behavior. |
| [LayoutDirty-Design.md](LayoutDirty-Design.md) | Layout dirty classification, retained publication ownership, and StyleOnly/partial-apply boundary. |
| [Diagnostics-Snapshot.md](Diagnostics-Snapshot.md) | Diagnostics snapshot boundary and formatter contract. |

One-off smoke/default evidence files are intentionally not canonical. Keep durable evidence in current regression commands, local guard summaries, and this status document.

---

## Current State

Private execution mode: these docs are working notes for a personal repository, not public compatibility or delivery commitments. Prefer direct target-architecture work, migrate internal APIs and tests when boundaries change, and keep secondary paths explicit and diagnostic-visible.

Latest remote quick CI completed successfully on 2026-06-09 for commit `a19bb30` ([run 27211735479](https://github.com/cloris-chan/Irix/actions/runs/27211735479)). Local commits after `a19bb30` and the current working-tree changes have not been remotely validated, so local gates are authoritative for the current tree. `.github/workflows/ci.yml` keeps push/PR validation to the quick Windows lane: SDK/Windows SDK check, Release build, and non-D3D12/non-performance/non-Guard tests. Heavier source/architecture guards, D3D12, glyph-atlas Smoke, performance, and AOT lanes remain available through local `Focused` / `GlyphSmoke` gates or `workflow_dispatch`; lower-frequency `DocGuard` wording and source-shape audits remain outside routine validation and are covered by the full test suite. Use local gates:

- Routine quick validation: `.\scripts\validate.ps1 -Mode Quick`.
- High-signal source/architecture guard, partial-apply, composition, and scroll/input preflight check: `.\scripts\validate.ps1 -Mode Focused`.
- Glyph atlas broad rendering check: `.\scripts\validate.ps1 -Mode GlyphSmoke`.
- Before and after broad changes: `.\scripts\glyph-atlas-regression.ps1 -Mode Smoke`.
- After glyph/page/shaping changes: `.\scripts\glyph-atlas-regression.ps1 -Mode Local`.
- After page-policy, eviction, or shaping overhauls: manually run `.\scripts\glyph-atlas-regression.ps1 -Mode Nightly`.
- Keep local `TestResults\glyph-atlas-regression-*-*.guard.summary.txt` as the active glyph-atlas status source.

| Area | Status |
|------|--------|
| Branch state | Renderer foundation and current D3D12 composition work are on `master`; the active head keeps direct/layer-content composition as the mainline and does not carry the removed viewport-space render-target cache. |
| Core foundation | Complete as a baseline, but not frozen. Reopen it when a target-architecture change is worth the migration cost; do not preserve old APIs for compatibility. |
| Windows backend | D3D12 is the active Windows PoC backend. GlyphAtlas is the only active text composition path. DirectWrite and WIC are allowed only as shaping, metrics, raster, and image source-data paths; final composition stays in the D3D12 command stream. |
| Glyph atlas | Coverage is guard-gated. New coverage should include matching oracle/regression/degradation checks; unguarded coverage expansion is rejected. SVG/COLR paint-tree-only glyphs, BiDi beyond current resolved-level projection, unsafe AtlasFull cases, and init/record failure remain explicit D3D12-only degradation until targeted. |
| Text/value IR | Framework/core paths must not retain raw text strings. Retained and drawing paths use `TextNodeContent`, `TextBufferSnapshot`, `FrameTextArena`, `TextSlice`, and resolver boundaries. CLI/debug/report formatting strings are output-boundary exceptions. |
| Color pipeline | The public semantic background and border paint vertical slices are implemented. `Color` and `SrgbColor` are public canonical/SDR bridge values; public `Paint` supports solid color and two-stop linear gradients in four element-bounds-relative directions; typed `BorderStroke` binds paint to one finite positive logical thickness. `VirtualNodeProperty.Background` stores `PropertyValue.Paint`, and `VirtualNodeProperty.Border` stores `PropertyValue.BorderStroke`. Draw recording resolves semantic directions against actual layout bounds, emits internal command-local `DrawMaterial`, retains gradients in `FrameDrawingResources`, and records one logical inward `StrokeRect` for a border. D3D12 FillRect/StrokeRect rasterize gradients through per-corner SDR vertex colors; stroke expansion uses four inward edges, continuous outer-bounds gradient coordinates, and independent X/Y DPI thickness. Retained/layer-cache payloads preserve command kind, material identity, and stroke width. Legacy window output preserves thickness and uses deterministic canonical midpoint fallback through `ColorOutputMapping.SdrSrgb`. Renderer materials, brush handles, backend capabilities, radial gradients, images, shared material resources, arbitrary stops/transforms, foreground paint, HDR/scRGB/Rec.2100 HLG/PQ output, system SDR brightness, per-screen mapping, and tone mapping remain internal or deferred. |
| Architecture boundary | `Irix.Poc` is an app, diagnostics, and adapter-glue project. It is not the reusable framework home. `D3D12DrawingBackend` has moved to `Irix.Platform.Windows`; `TranslatorCore`, `TranslatorInput`, `TranslatorOutput`, and `TranslatorRetainedState` have moved to `Irix.Rendering`; `WindowDrawCommandTranslator` remains the Poc adapter around viewport timing, app/control scroll feedback, diagnostics, allocation attribution, and Counter default composition. The scroll/input runtime owner boundary is written: future extraction needs explicit logical-state, feedback-sink, compositor-sampler, hit-test-service, input-action-mapper, and app-message-dispatch adapters. The first Poc feedback sink, internal app dispatch intent shape, wheel-input dispatch intent shape, max-scroll feedback dispatch mapper, scroll-presentation interrupt mapper path, hit-test service, input action mapper, input-route app-message dispatch mapper, wheel-input scroll presentation dispatch sink, app runtime dispatch sink, marker-runtime dispatch-through-sink, native-input event pump, and `PocRuntimeSettings` composition-root settings snapshot exist. The Windows native input callback now only enqueues for the Poc pump before returning; runtime/compositor input work stays off the Win32 platform thread, and the pump awaits the current async dispatch before consuming the next event to preserve FIFO. `AppDispatchIntent`, `WheelInputDispatchIntent`, and `PocRuntimeSettings` only group Poc dispatch/options before Counter message mapping, scroll presentation delivery, or runtime startup wiring; they are not public contracts, diagnostics channels, event buses, provider APIs, or extraction licenses. Source guards now cover marker-runtime dispatch-through-sink source guard and broader Counter/Poc runtime identity leaks before extraction. `WindowBackend` stays as legacy/debug presentation. |
| Style / animation / composition | Style remains an internal renderer IR, not a public UI style API: the first internal semantic declaration layer (`StylePropertyId` / `StyleValue` / `StyleDeclaration` / `StyleDeclarationMapper`) now maps width, height, background, foreground, opacity, translation, and control-state style terms one-way into `VirtualNodeProperty` without exposing layout/visual/composition processing categories. Semantic visual color properties use canonical typed color storage, classify as visual-only, and can override rectangle/button/text draw-command colors without changing layout geometry; opacity/translation declarations map to composition metadata with a separate composition mask, explicit `StyleDeltaPlan` work flags, and a preflight compiler that can turn pure opacity/translation deltas into existing transform/opacity declarations. The public style/transition authoring preflight is documented and guard-covered: future UI authoring stays semantic, does not expose layout/visual/composition processing categories, and keeps transition start/cancel/retarget/commit ownership in the runtime rather than the compiler or diagnostics. A narrow Poc-owned style transition runtime path now exists: runtime decisions feed a coordinator, retained snapshots gate compositor install, start/retarget compile through `StyleTransitionCompiler`, cancel/commit clear transform/opacity presentation, and rejected draw/layout-owned deltas fallback before ownership changes. Counter app/control integration now maps single-target `OwnershipSnapshot` deltas through semantic declarations, keeps the narrow single-decision API for single-target deltas while resolving the Counter control-state owner key for completion tracking, and routes multi-target button control-state deltas through a Poc-owned `StyleTransitionDecisionBatch`. The Counter bridge filters transition entries by actual transform/opacity delta, so focus-only changes stay on normal visual rendering and do not turn hovered cross-focus button press into a presentation-set batch. `CounterStyleTransitionRuntimeBridge` waits for app render publication, then either installs a single-owner declaration or activates an all-ready presentation-set batch through the existing concurrent-owner preflight. The main-app input transition route retimestamps after retained publication, anchors a small visible initial sample, and renders one best-effort compositor tick immediately after activation so short press transitions do not depend on the background pump's next scheduled tick. Active scroll presentation still uses normal app dispatch with explicit presentation cleanup while the scroll schedule is active; wheel input now also aborts any active style transition presentation before starting scroll presentation, retained last-presented samples from completed scroll presentation no longer block the first post-scroll button press transition, the Poc input pump preserves native-event FIFO across async style/scroll dispatch, and blocked batch activation clears stale style transition presentation/tracker state after app state publication. A Poc-owned marker-driven completion tracker (`StyleTransitionCompletionTracker`) decorates `Once` transform/opacity start/retarget decisions with a progress-1 marker, stores active entries in an owner table keyed by `StyleTransitionOwnerKey`, supersedes an older active owner for the same target when a new control-state owner takes over, and converts matching compositor marker events into explicit runtime `Commit` decisions without letting one owner clear another target. Whole-presentation abort now clears the owner table. `StyleTransitionCompletionPump` connects that tracker to the Counter main-app path by running transform/opacity or active presentation-set compositor ticks, draining marker events, and applying or recording explicit commits through the coordinator boundary; it refreshes tick mode from the current compositor plan while running, and its internal lifecycle snapshot/formatter covers no-active, tick-rendered, completion-committed, tick-unavailable, error states, and active owner count without creating a public scheduler. The true concurrent multi-owner transition path now has Poc-owned owner keys, decision batches, per-owner validation results, deterministic acceptance/rejection tests, coordinator `ValidateBatch` coverage, validation-only `ValidateBatchRuntime` requirements pairing, compositor-side presentation-set validation/conflict reporting, rendering-owned activation preflight/plan-set data, active presentation-set compositor state/tick contract, Poc-owned all-ready start/retarget activation bridge, Poc-owned all-ready cancel/commit clear bridge, per-owner active presentation clear, active presentation-set completion pumping, owner-table completion tracking with same-target owner supersession, and Counter multi-target routing over that path. The internal `CompositionClock` boundary now backs `CompositionTimestamp.Now()` as the single high-precision animation timeline source; `DrawingBackendCompositor`, `CompositorLoop`, `StyleTransitionCompletionPump`, and `ScrollPresentationCoordinator` sample the same internal `ICompositionClockSource` boundary for compositor-owned ticks, delayed scheduling, automatic completion ticks, and scroll retarget segment starts; timestamp arithmetic saturates at extreme values so timeline sampling clamps instead of wrapping progress back to an earlier state; multi-output and heterogeneous refresh-rate rendering must treat backend present/vblank/fence timing as pacing and diagnostics only, so equal `CompositionTimestamp` samples produce equal animation state across outputs. There is still no generic scheduler, public transition API, theme/cascade, loop/alternate auto-completion, arbitrary app-level multi-owner route, or full multi-output compositor scheduler. D3D12 composition covers retained `NodeKey` transform/opacity declarations, fixed-clip scroll presentation declarations, multi-layer nested/mixed-clip decomposition, compositor-only ticks, active presentation-set transform/opacity ticks, marker events plus runtime dispatch pumping, active hit-test remapping, main-app scroll presentation integration, reason-typed lifecycle cancellation, skip diagnostics, and layer content caching. Backend active plan exclusivity across single animation, presentation-set animation, and scroll presentation is now held behind private `DrawingBackendCompositor` presentation state with marker playback, last-sample, pending retained scroll-refresh state, and shared execution preflight/skip recording for compositor-only ticks plus retained scroll refresh; typed plan validation remains in the typed tick/refresh paths, retained-frame refresh remains scroll-presentation-specific, and neither path is a generic runtime scheduling channel. Recent hardening pins fixed-clip hit-test occlusion, nested layer hit-test remapping, device-recovery cache clearing, cache key invalidation, fixed-clip cache-hit state, empty fixed-clip intersections, layer-cache paint order with interleaved ordinary commands, sibling-layer hit-test routing, mapped/unmapped marker runtime dispatch including cross-batch drain/unmapped accounting, empty sample-and-hold without cancellation, superseded/sample-hold/lifecycle scroll-presentation idle waiters, invalidation-after-explicit-cancel non-double-counting, loop-owned active/held/queued scroll presentation lifecycle state, retained-frame staging composition refresh/defer mode, lifecycle stale queued tick suppression, scroll presentation plan-loss stale tick suppression, scroll marker cancellation queue cleanup, completed scroll presentation active-schedule versus retained-sample separation, retained-frame text/content updates that can resolve the same scroll target and command shape without canceling the active scroll presentation, non-finite/zero/counteracting pending pixel suppression across scroll presentation and the legacy scroll frame pump, active-loop reverse-input retargeting from the latest compositor sample, zero-pixel wheel dispatch suppression, invalid max-scroll feedback suppression/normalization, ScrollController numeric state/delta/tick/presented/max-scroll recovery, composition scalar animation/tick output recovery, composition timestamp arithmetic/timeline overflow recovery, and composition layer transform/opacity/fixed-clip value recovery. Internal offscreen/render-target caching is not active; secondary paths require documented blockers. |
| Partial apply / StyleOnly | `RenderPipeline.Build` now has a guarded StyleOnly layout-skip branch: proven style-only dirty sets reuse retained layout geometry/tree/scroll diagnostics, patch current text handles plus dirty visual/action metadata through sorted retained-DFS dirty projection, re-record current-frame commands/resources, rebuild hit targets, and preserve dirty ranges without incrementing `LayoutRebuildCount`. Semantic background paint and border stroke refresh through the same fast path, dirty element to command-range validation is shared in `RangeUtils`, and retained layout patching, retained root metadata projection, and hit-target metadata projection share duplicate/stale/missing dirty-index cursor behavior. Text-size, layout, tree, viewport, incomplete dirty projection, and unsafe retained metadata paths fall back to full layout. Retained partial apply and segmented render-source handoff remain post-publication command/resource/hit-target reuse paths: the Poc runtime enables the selected segmented render-source path by default, `--no-partial-apply` disables it, and guarded full-frame fallback remains the baseline when ownership, freshness, or range validation rejects the partial path. Optional diagnostics expose a stable partial handoff status line for disabled, executed, fallback, and rejected production-owner states. |
| Performance | Allocation measurement/hardening is on hold. Latest `--diagnose-text-cache 180` warm scroll baseline with buildRoot/layout attribution is the comparison point: about `396760 bytes`, `2204 B/frame`; `pipeline.layout=501` B/frame, `tree.buildRoot=546` B/frame, and record about `45 B/frame`. Only safe empty publication array reuse was implemented. Remaining tree/layout/snapshot buckets require ownership design, not opportunistic retained-array micro-optimization. |
| Diagnostics | Optional diagnostics are built with `IrixDiagnostics=true`, compile out by default, and use separate `bin/diagnostics` / `obj/diagnostics` outputs from normal `bin/runtime` / `obj/runtime` builds to avoid stale mixed-ABI incremental outputs. A guard-scoped scenario transcript fixture now reuses existing snapshots and formatters for scroll presentation continuity, style transition completion, and material output diagnostics; expand it only for concrete regressions and keep unified diagnostics channel work postponed. |
| Packaging | NuGet preview package ID reservation is prepared but not a stable public release. `Irix.Poc` is explicitly non-packable, library packages carry preview metadata without package readme metadata or packaged readme content, the `Irix` entry metapackage provides the no-suffix consumer package, `scripts\pack-nuget-preview.ps1` verifies package shape and rejects accidental readme metadata/content, and `scripts\package-consumer-smoke.ps1` rebuilds the Poc source against the local `Irix` package before any publish. `.github/workflows/release.yml` uses NuGet Trusted Publishing through `NuGet/login@v1`, publishes manually requested preview versions, uploads the packed artifacts, and unlists the packages by default after publish. Root `README.md` remains intentionally absent while the repository can be public for Actions/Trusted Publishing infrastructure. |
| Docs | Current docs describe active architecture and remaining work, not process history. |

## Current Design Notes

- The Counter PoC button currently applies press `TranslateY` to the same visual node that carries action/hit-test metadata. This intentionally exercises active/presented hit-test remapping: pointer input can target the visual position while a transform animation is in flight. Top/bottom edge hover or pressed-state flicker during that PoC press animation is not tracked as a current runtime bug. Future public controls should keep the outer interaction, capture, focus, and release-activation bounds stable, and apply hover/press motion to an inner visual/content layer so visual feedback does not move the control's own activation boundary.

Performance allocation note: `layout.nodeWalk=0` means the layout bucket is retained publication cost, not property-read or clip-propagation allocation. `LayoutTreeResult` arrays are retained across frames and must not expose pooled mutable storage. `tree.buildRoot` attribution is diagnostic-only; it does not change `VirtualNode` or builder APIs. Reopen retained-array, layout-result, or snapshot work only with a written ownership design and one measured target bucket.

---

## Active Source Guards

Keep guards green for these non-negotiable boundaries:

- Final text composition stays on the D3D12 GlyphAtlas path; DirectWrite/WIC remain source-data providers.
- No runtime shader compile in active renderer source.
- No raw retained text string path in framework/core rendering or layout state.
- No public string style/value factory path, no public renderer brush/material/radial-gradient/image authoring path, no public UI authoring split by layout/visual/composition processing layers, no public transition scheduler/theme/cascade surface, no legacy attribute-era API, public `VirtualNodeProperty` construction, silent `PropertyValue` getters, ref-struct leakage into retained/batch state, or framework/backend `record struct` outside the existing PoC authoring exception.
- No treating current `DrawColor` / `WindowColor` SDR payloads as the canonical Irix color value; canonical color work must preserve the [Color-Pipeline.md](Color-Pipeline.md) boundary.
- No internal material kind beyond `None`, `SolidColor`, and `LinearGradient`; public non-solid authoring is limited to semantic two-stop directional background paint and typed border stroke until a dedicated slice expands the contract.
- No HDR/scRGB/Rec.2100 output mapper, D3D12 HDR swapchain format, D3D12 color-space selection, system SDR brightness, or tone-mapping implementation before the guarded HDR output mapping slice exists.
- No per-monitor, backend-present, vblank, fence, or refresh-rate animation clock; those signals are pacing/diagnostics only and all animation progress must flow through the Irix composition clock / `CompositionTimestamp` boundary.
- No compositor/runtime code extraction before the relevant style, animation, composition, scroll, or input/control ownership contract is written.
- No viewport-space internal offscreen/render-target cache on the active composition path.

---

## Next Reasonable Work

| Priority | Work | Boundary |
|----------|------|----------|
| P0 | Keep local Smoke gate authoritative | Run `.\scripts\glyph-atlas-regression.ps1 -Mode Smoke` for broad rendering changes; push/PR quick CI does not cover D3D12 or glyph-atlas behavior. |
| P0 | Keep local validation entrypoint authoritative | Use `.\scripts\validate.ps1 -Mode Quick` for routine validation and `.\scripts\validate.ps1 -Mode Focused` after high-signal source/architecture guard, diagnostics, partial-apply, composition, or scroll/input changes. Use the full suite when lower-frequency `DocGuard` wording or source-shape audits matter. |
| P1 | Direct composition hardening | Keep transform/opacity, fixed-clip scroll, nested/mixed-clip layers, hit testing, marker dispatch, layer content cache, and skip diagnostics on the direct D3D12 path. Backend execution preflight/skip recording is centralized privately in `DrawingBackendCompositor` without adding a public diagnostics contract or runtime scheduler surface. Composition scalar animation evaluation now recovers non-finite progress/endpoints into finite tick output, timestamp arithmetic saturates extreme elapsed samples, composition layer values normalize non-finite transform/opacity input, and scroll presentation declaration resolution rejects non-finite presented ranges before install. Layer-cache paint order, marker skip/cancellation queue cleanup, and marker pump cross-batch mapped/unmapped accounting now have narrow coverage; add more regression coverage only when new concrete behavior boundaries appear. |
| P1 | Style transition runtime hardening | Internal semantic style declarations now feed the existing renderer IR and Poc control-state/transition adapters. Public authoring, Poc-owned runtime ownership, Counter app/control transition integration, lifecycle preflight, marker-driven completion tracker, main-app completion pump, active-scroll fallback cleanup, completion-pump lifecycle diagnostics, true concurrent multi-owner transition design preflight, Poc-owned batch/owner value types plus deterministic acceptance/rejection tests, coordinator batch validation against one retained snapshot, validation-only batch runtime preflight, compositor-side presentation-set validation/conflict reporting without install, rendering-owned activation preflight/plan-set data, active presentation-set compositor state/tick contract, Poc-owned all-ready start/retarget activation bridge, Poc-owned all-ready cancel/commit clear bridge, active presentation-set completion pumping, owner-table completion tracking with same-target owner supersession, per-owner active presentation clear, Counter multi-target transition routing, running-pump single-to-presentation tick-mode switching, routed multi-target completion tick-mode diagnostics, and blocked fallback batch activation diagnostics now use the existing concurrent-owner preflight. Expand style only around concrete main-app regressions or the color/material contract; still no theme/cascade, public transition API, generic scheduler, arbitrary app-level multi-owner route, or public processing-layer authoring names. |
| P1 | Color draw/material payload migration | Public canonical color values plus semantic background paint and typed inward border stroke are implemented end to end: authoring, style/property storage, retained brush material, D3D12 FillRect/StrokeRect rasterization and layer-cache retention, asymmetric DPI stroke scaling, and deterministic legacy fallback with preserved thickness. The remaining color slices are richer gradient stops/transforms, radial/image/shared material resources, foreground paint, and HDR backend/compositor output mapping. Leave HDR/scRGB/Rec.2100 support, per-monitor color mapping, system SDR brightness, and tone mapping to dedicated slices. |
| P1 | Main-app composition integration | Main app wheel input starts compositor scroll presentation ticks; hover/press/release can route through active presentation; focused keyboard activation can update retained text/content without canceling the active scroll presentation when the target and command shape remain continuous; native input enters through a Poc-owned single-reader pump so the Win32 platform thread only enqueues and returns while async dispatch preserves FIFO; chained retarget distance, compositor-thread sample-and-hold continuity, `EnsureRunning` rapid wheel coalescing, active-loop reverse wheel retargeting from the latest compositor sample, wheel-before-scroll style-transition abort, completed-scroll active-schedule versus retained-sample separation for first post-scroll button press transitions, top/bottom boundary wheel clamping, resize/DPI/max-scroll lifecycle cancellation, lifecycle idle waiter release, invalidation-after-explicit-cancel non-double-counting, loop-owned active/held/queued lifecycle state, stale queued tick suppression, compositor-plan-lost stale tick suppression, retained text/content continuity during active scroll, non-finite/zero/counteracting pending pixel suppression across the presentation coordinator and legacy scroll frame pump, zero-pixel wheel dispatch suppression, invalid max-scroll feedback suppression/normalization, and ScrollController numeric state/delta/tick/presented/max-scroll recovery are diagnostic/test covered. Continue hardening cancellation, retarget, hit-test, interaction, and marker behavior without adding a generic fallback compositor. |
| P1 | Scenario diagnostics fixture | Guard-scoped transcript coverage exists for scroll presentation continuity, style transition completion, and material output diagnostics. Add scenarios only for concrete regressions; do not add a new CLI surface, golden files, public diagnostics API, or unified diagnostics channel. |
| P1 | NuGet preview package reservation | Publish only manually triggered unlisted preview packages via Trusted Publishing after `pack-nuget-preview.ps1`, `package-consumer-smoke.ps1`, and the normal validation lane pass. Package readme metadata/content is intentionally absent while this is only package-ID reservation infrastructure. Do not add root launch-style README content or compatibility claims before the framework boundary is ready. |
| P1 | Framework ownership contracts | `D3D12DrawingBackend` and translator core promotion are complete. `WindowBackend` stays in Poc. Scroll/input ownership is documented in `Poc-Promotion-Contracts.md`, and Poc-owned dispatch intent shapes now make input ownership changes, routed input, max-scroll feedback, scroll-presentation interrupt decisions, and wheel raw-to-pixel dispatch explicit before `CounterMessage` mapping or scroll presentation delivery. Do not move runtime types until a separate extraction commit introduces the required adapters and removes Counter-specific message/action assumptions. |
| P1 | Allocation follow-up hold | Keep the current `--diagnose-text-cache 180` baseline as a future comparison point. Do not resume retained array, snapshot, or tree-builder optimization without an ownership design and a single measured target bucket. |
| P1 | Glyph atlas expansion / maintenance | Add coverage aggressively when it is target-architecture work and includes matching oracle/regression/degradation checks; reject unguarded coverage drift. |
| P2 | Entry-level eviction | Keep design-only until retained atlas command ownership and sub-rect free-list ownership are explicit. |
| P2 | Pixel/layout oracle | Separate future work; do not block the current D3D12-only renderer foundation. |
