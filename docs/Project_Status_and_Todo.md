# Irix Project Status

> Current developer handoff note for the Irix Windows PoC.
> Last verified: 2026-06-07.

---

## Canonical Docs

| Doc | Purpose |
|-----|---------|
| [Irix_Framework_Design.md](Irix_Framework_Design.md) | Main architecture design, layer boundaries, ADR index, and private execution scope. |
| [Active-Worklist.md](Active-Worklist.md) | Current private work items and framework-promotion candidates. |
| [Style-System.md](Style-System.md) | Style layer contract: layout style, visual style, text shaping style, composition style, and control-state style. |
| [Animation-Composition.md](Animation-Composition.md) | Animation ownership contract: UI-runtime animation, compositor animation, hybrid animation, and scroll presentation model. |
| [GPU-Composition-Architecture.md](GPU-Composition-Architecture.md) | Future platform-neutral composition/GPU-offload architecture for D3D12 now and Vulkan/Metal later. |
| [D3D12-Composition.md](D3D12-Composition.md) | Active D3D12 composition implementation for transform/opacity and fixed-clip scroll ticks over retained draw output. |
| [Color-Pipeline.md](Color-Pipeline.md) | Canonical Irix color contract: ideal linear BT.2020 value, current SDR/sRGB output stage, and deferred HDR output mapping owner. |
| [Poc-Promotion-Contracts.md](Poc-Promotion-Contracts.md) | Promotion contracts for `Irix.Poc` runtime adapters, Windows backend adapters, and legacy/debug presentation paths. |
| [Glyph-Atlas-Design.md](Glyph-Atlas-Design.md) | D3D12 glyph atlas text renderer design, degradation policy, and guarded coverage expansion. |
| [Glyph-Atlas-Entry-Eviction-Design.md](Glyph-Atlas-Entry-Eviction-Design.md) | Design-only preconditions and non-goals for future entry-level LRU / sub-rect free-list work. |
| [ADR-Scissor-Clipping.md](ADR-Scissor-Clipping.md) | Clip/scissor/text-clip decision and guarded behavior. |
| [LayoutDirty-Design.md](LayoutDirty-Design.md) | Layout dirty classification, retained publication ownership, and StyleOnly/partial-apply boundary. |
| [Diagnostics-Snapshot.md](Diagnostics-Snapshot.md) | Diagnostics snapshot boundary and formatter contract. |

One-off smoke/default evidence files are intentionally not canonical. Keep durable evidence in current regression commands, local guard summaries, and this status document.

---

## Current State

Private execution mode: these docs are working notes for a personal repository, not public compatibility or delivery commitments. Prefer direct target-architecture work, migrate internal APIs and tests when boundaries change, and keep secondary paths explicit and diagnostic-visible.

GitHub Actions quota is currently exhausted. `.github/workflows/ci.yml` now keeps push/PR validation to the quick Windows lane: SDK/Windows SDK check, Release build, and non-D3D12/non-performance tests. Heavier D3D12, glyph-atlas Smoke, performance, and AOT lanes remain available through `workflow_dispatch`, but Actions status is not the source of truth until quota returns. Use local gates:

- Routine source/diagnostics check: `.\scripts\validate.ps1 -Mode Quick`.
- Architecture-boundary, partial-apply, composition, and scroll/input preflight check: `.\scripts\validate.ps1 -Mode Focused`.
- Glyph atlas broad rendering check: `.\scripts\validate.ps1 -Mode GlyphSmoke`.
- Before and after broad changes: `.\scripts\glyph-atlas-regression.ps1 -Mode Smoke`.
- After glyph/page/shaping changes: `.\scripts\glyph-atlas-regression.ps1 -Mode Local`.
- After page-policy, eviction, or shaping overhauls: manually run `.\scripts\glyph-atlas-regression.ps1 -Mode Nightly`.
- Keep local `TestResults\glyph-atlas-regression-*-*.guard.summary.txt` as the active glyph-atlas status source.

| Area | Status |
|------|--------|
| Branch state | Renderer foundation and current D3D12 composition work are on `master`; the active head keeps direct/layer-content composition as the mainline and does not carry the removed viewport-space render-target cache. |
| Core foundation | Complete as a baseline, but not frozen. Reopen it when a target-architecture change is worth the migration cost; do not preserve old APIs for compatibility. |
| Windows backend | D3D12 is the active Windows PoC backend. GlyphAtlas is the only active text composition path. DirectWrite and WIC are allowed only as shaping, metrics, raster, and image source-data paths; final composition stays in the D3D12 command stream. |
| Glyph atlas | Coverage is guard-gated. New coverage should include matching oracle/regression/degradation checks; unguarded coverage expansion is rejected. SVG/COLR paint-tree-only glyphs, BiDi beyond current resolved-level projection, unsafe AtlasFull cases, and init/record failure remain explicit D3D12-only degradation until targeted. |
| Text/value IR | Framework/core paths must not retain raw text strings. Retained and drawing paths use `TextNodeContent`, `TextBufferSnapshot`, `FrameTextArena`, `TextSlice`, and resolver boundaries. CLI/debug/report formatting strings are output-boundary exceptions. |
| Color pipeline | The first SDR/sRGB slice is implemented: internal `Color` is a 16-byte canonical linear BT.2020 / Rec.2020 straight-alpha value, `StyleColor` wraps it, and `PropertyValue.Color` stores it instead of only `uint ARGB`. sRGB authoring enters through `FromSrgb` / `StyleColor.FromArgb` / `StyleColor.Opaque`; draw commands now retain canonical color/material internally, internal solid-color and first linear-gradient `DrawMaterial`/brush resource shapes exist for layer-cache payloads, D3D12 FillRect can rasterize clamp-free linear gradients through per-corner SDR vertex-color interpolation with center-sample representative color, treats degenerate equal-point gradients as start-color single rects, and uses bounded segmented fallback when clamp boundaries cross the fill, D3D12 text and legacy window output still downgrade unsupported non-solid material paths through deterministic SDR fallback, D3D12 material output diagnostics expose selected material kind/backend capability/fallback reason/fallback counts plus linear-gradient single-rect versus segmented rasterization counters for direct and layer-cache paths, public material authoring is guard-deferred from style/property APIs and now has a policy preflight requiring semantic paint intent instead of renderer-owned `DrawMaterial`/brush handles/backend capability flags, the non-solid material policy preflight names the required lifetime/coordinate/interpolation/invalidation/backend-fallback/diagnostics contracts, the HDR output mapping preflight names the required output-context/screen-region/SDR-white/tone-mapping/swapchain-capability/payload-format/diagnostics/fallback contracts, and D3D12 plus legacy window output downgrade through the internal `ColorOutputMapping.SdrSrgb` boundary into explicit SDR bridges such as `DrawColor`, `WindowColor`, and the current D3D12 R8G8B8A8 path. Public material authoring implementation, richer gradient/image rasterization, HDR/scRGB/Rec.2100 HLG/PQ output, system SDR brightness, per-screen mapping, and tone mapping are still not implemented. |
| Architecture boundary | `Irix.Poc` is an app, diagnostics, and adapter-glue project. It is not the reusable framework home. `D3D12DrawingBackend` has moved to `Irix.Platform.Windows`; `TranslatorCore`, `TranslatorInput`, `TranslatorOutput`, and `TranslatorRetainedState` have moved to `Irix.Rendering`; `WindowDrawCommandTranslator` remains the Poc adapter around viewport timing, app/control scroll feedback, diagnostics, allocation attribution, and Counter default composition. The scroll/input runtime owner boundary is written: future extraction needs explicit logical-state, feedback-sink, compositor-sampler, hit-test-service, input-action-mapper, and app-message-dispatch adapters. The first Poc feedback sink, max-scroll feedback dispatch mapper, hit-test service, input action mapper, input-route app-message dispatch mapper, wheel-input scroll presentation dispatch sink, app runtime dispatch sink, and marker-runtime dispatch-through-sink cuts exist. Source guards now cover marker-runtime dispatch-through-sink source guard and broader Counter/Poc runtime identity leaks before extraction. `WindowBackend` stays as legacy/debug presentation. |
| Style / animation / composition | Style remains an internal renderer IR, not a public UI style API: the first internal semantic declaration layer (`StylePropertyId` / `StyleValue` / `StyleDeclaration` / `StyleDeclarationMapper`) now maps width, height, background, foreground, opacity, translation, and control-state style terms one-way into `VirtualNodeProperty` without exposing layout/visual/composition processing categories. Semantic visual color properties use canonical typed color storage, classify as visual-only, and can override rectangle/button/text draw-command colors without changing layout geometry; opacity/translation declarations map to composition metadata with a separate composition mask, explicit `StyleDeltaPlan` work flags, and a preflight compiler that can turn pure opacity/translation deltas into existing transform/opacity declarations. The public style/transition authoring preflight is documented and guard-covered: future UI authoring stays semantic, does not expose layout/visual/composition processing categories, and keeps transition start/cancel/retarget/commit ownership in the runtime rather than the compiler or diagnostics. A narrow Poc-owned style transition runtime path now exists: runtime decisions feed a coordinator, retained snapshots gate compositor install, start/retarget compile through `StyleTransitionCompiler`, cancel/commit clear transform/opacity presentation, and rejected draw/layout-owned deltas fallback before ownership changes. Counter app/control integration now maps single-target `OwnershipSnapshot` deltas through semantic declarations and routes multi-target button control-state deltas through a Poc-owned `StyleTransitionDecisionBatch`; the legacy single-decision API stays narrow. `CounterStyleTransitionRuntimeBridge` waits for app render publication, then either installs a single-owner declaration or activates an all-ready presentation-set batch through the existing concurrent-owner preflight. Active scroll presentation still uses normal app dispatch with explicit presentation cleanup, and blocked batch activation clears stale style transition presentation/tracker state after app state publication. A Poc-owned marker-driven completion tracker (`StyleTransitionCompletionTracker`) decorates `Once` transform/opacity start/retarget decisions with a progress-1 marker, stores active entries in an owner table keyed by `StyleTransitionOwnerKey`, and converts matching compositor marker events into explicit runtime `Commit` decisions without letting one owner clear another. Whole-presentation abort now clears the owner table. `StyleTransitionCompletionPump` connects that tracker to the Counter main-app path by running transform/opacity or active presentation-set compositor ticks, draining marker events, and applying or recording explicit commits through the coordinator boundary; its internal lifecycle snapshot/formatter covers no-active, tick-rendered, completion-committed, tick-unavailable, error states, and active owner count without creating a public scheduler. The true concurrent multi-owner transition path now has Poc-owned owner keys, decision batches, per-owner validation results, deterministic acceptance/rejection tests, coordinator `ValidateBatch` coverage, validation-only `ValidateBatchRuntime` requirements pairing, compositor-side presentation-set validation/conflict reporting, rendering-owned activation preflight/plan-set data, active presentation-set compositor state/tick contract, Poc-owned all-ready start/retarget activation bridge, Poc-owned all-ready cancel/commit clear bridge, active presentation-set completion pumping, owner-table completion tracking, and Counter multi-target routing over that path. The internal `CompositionClock` boundary now backs `CompositionTimestamp.Now()` as the single high-precision animation timeline source; `DrawingBackendCompositor`, `CompositorLoop`, `StyleTransitionCompletionPump`, and `ScrollPresentationCoordinator` sample the same internal `ICompositionClockSource` boundary for compositor-owned ticks, delayed scheduling, automatic completion ticks, and scroll retarget segment starts; multi-output and heterogeneous refresh-rate rendering must treat backend present/vblank/fence timing as pacing and diagnostics only, so equal `CompositionTimestamp` samples produce equal animation state across outputs. There is still no generic scheduler, public transition API, theme/cascade, loop/alternate auto-completion, per-owner compositor partial clear, arbitrary app-level multi-owner route, or full multi-output compositor scheduler. D3D12 composition covers retained `NodeKey` transform/opacity declarations, fixed-clip scroll presentation declarations, multi-layer nested/mixed-clip decomposition, compositor-only ticks, active presentation-set transform/opacity ticks, marker events plus runtime dispatch pumping, active hit-test remapping, main-app scroll presentation integration, reason-typed lifecycle cancellation, skip diagnostics, and layer content caching. Recent hardening pins fixed-clip hit-test occlusion, nested layer hit-test remapping, device-recovery cache clearing, cache key invalidation, fixed-clip cache-hit state, empty fixed-clip intersections, layer-cache paint order with interleaved ordinary commands, sibling-layer hit-test routing, mapped/unmapped marker runtime dispatch including cross-batch drain/unmapped accounting, empty sample-and-cancel without cancellation, superseded/sample-and-cancel/lifecycle scroll-presentation idle waiters, invalidation-after-explicit-cancel non-double-counting, lifecycle stale queued tick suppression, and scroll marker cancellation queue cleanup. Internal offscreen/render-target caching is not active; secondary paths require documented blockers. |
| Partial apply / StyleOnly | `RenderPipeline.Build` still rebuilds `LayoutTreeBuilder` output when tree, viewport, or dirty nodes require layout; there is no active StyleOnly layout-skip branch inside the pipeline. Retained partial apply and segmented render-source handoff are implemented for command/resource/hit-target metadata reuse after publication: the Poc runtime enables the selected segmented render-source path by default, `--no-partial-apply` disables it, and guarded full-frame fallback remains the baseline when ownership, freshness, or range validation rejects the partial path. Optional diagnostics now expose a stable partial handoff status line for disabled, executed, fallback, and rejected production-owner states. |
| Performance | Allocation measurement/hardening is on hold. Latest `--diagnose-text-cache 180` warm scroll baseline with buildRoot/layout attribution is the comparison point: about `396760 bytes`, `2204 B/frame`; `pipeline.layout=501` B/frame, `tree.buildRoot=546` B/frame, and record about `45 B/frame`. Only safe empty publication array reuse was implemented. Remaining tree/layout/snapshot buckets require ownership design, not opportunistic retained-array micro-optimization. |
| Diagnostics | Optional diagnostics are built with `IrixDiagnostics=true`, compile out by default, and use separate `bin/diagnostics` / `obj/diagnostics` outputs from normal `bin/runtime` / `obj/runtime` builds to avoid stale mixed-ABI incremental outputs. |
| Docs | Current docs describe active architecture and remaining work, not process history. |

Performance allocation note: `layout.nodeWalk=0` means the layout bucket is retained publication cost, not property-read or clip-propagation allocation. `LayoutTreeResult` arrays are retained across frames and must not expose pooled mutable storage. `tree.buildRoot` attribution is diagnostic-only; it does not change `VirtualNode` or builder APIs. Reopen retained-array, layout-result, or snapshot work only with a written ownership design and one measured target bucket.

---

## Active Source Guards

Keep guards green for these non-negotiable boundaries:

- Final text composition stays on the D3D12 GlyphAtlas path; DirectWrite/WIC remain source-data providers.
- No runtime shader compile in active renderer source.
- No raw retained text string path in framework/core rendering or layout state.
- No public string style/value factory path, no public style/property brush/material/gradient/image authoring path before the material policy exists, no public UI authoring split by layout/visual/composition processing layers, no public transition scheduler/theme/cascade surface, no legacy attribute-era API, public `VirtualNodeProperty` construction, silent `PropertyValue` getters, ref-struct leakage into retained/batch state, or framework/backend `record struct` outside the existing PoC authoring exception.
- No treating current `DrawColor` / `WindowColor` SDR payloads as the canonical Irix color value; canonical color work must preserve the [Color-Pipeline.md](Color-Pipeline.md) boundary.
- No internal material kind beyond `None`, `SolidColor`, and `LinearGradient`, and no public material/gradient/image authoring implementation, before the dedicated material implementation slice expands the contract.
- No HDR/scRGB/Rec.2100 output mapper, D3D12 HDR swapchain format, D3D12 color-space selection, system SDR brightness, or tone-mapping implementation before the guarded HDR output mapping slice exists.
- No per-monitor, backend-present, vblank, fence, or refresh-rate animation clock; those signals are pacing/diagnostics only and all animation progress must flow through the Irix composition clock / `CompositionTimestamp` boundary.
- No compositor/runtime code extraction before the relevant style, animation, composition, scroll, or input/control ownership contract is written.
- No viewport-space internal offscreen/render-target cache on the active composition path.

---

## Next Reasonable Work

| Priority | Work | Boundary |
|----------|------|----------|
| P0 | Keep local Smoke gate authoritative | Run `.\scripts\glyph-atlas-regression.ps1 -Mode Smoke` for broad changes while Actions quota is unavailable. |
| P0 | Keep local validation entrypoint authoritative | Use `.\scripts\validate.ps1 -Mode Quick` for routine validation and `.\scripts\validate.ps1 -Mode Focused` after architecture-boundary, diagnostics, partial-apply, composition, scroll/input, or docs/status changes. |
| P1 | Direct composition hardening | Keep transform/opacity, fixed-clip scroll, nested/mixed-clip layers, hit testing, marker dispatch, layer content cache, and skip diagnostics on the direct D3D12 path. Layer-cache paint order, marker skip/cancellation queue cleanup, and marker pump cross-batch mapped/unmapped accounting now have narrow coverage; add more regression coverage only when new concrete behavior boundaries appear. |
| P1 | Style transition runtime hardening | Internal semantic style declarations now feed the existing renderer IR and Poc control-state/transition adapters. Public authoring, Poc-owned runtime ownership, Counter app/control transition integration, lifecycle preflight, marker-driven completion tracker, main-app completion pump, active-scroll fallback cleanup, completion-pump lifecycle diagnostics, true concurrent multi-owner transition design preflight, Poc-owned batch/owner value types plus deterministic acceptance/rejection tests, coordinator batch validation against one retained snapshot, validation-only batch runtime preflight, compositor-side presentation-set validation/conflict reporting without install, rendering-owned activation preflight/plan-set data, active presentation-set compositor state/tick contract, Poc-owned all-ready start/retarget activation bridge, Poc-owned all-ready cancel/commit clear bridge, active presentation-set completion pumping, owner-table completion tracking, Counter multi-target transition routing, routed multi-target completion tick-mode diagnostics, and blocked fallback batch activation diagnostics now use the existing concurrent-owner preflight. Expand style only around concrete main-app regressions or the color/material contract; still no theme/cascade, public transition API, generic scheduler, per-owner compositor partial clear, or public processing-layer authoring names. |
| P1 | Color draw/material payload migration | Canonical internal `Color` exists for style/property values and draw commands carry canonical color/material internally. The active SDR/sRGB output stage now has an explicit internal `ColorOutputMapping.SdrSrgb` boundary used by D3D12 and legacy window output paths, layer-cache payloads can carry internal solid-color and first linear-gradient `DrawMaterial` values through the brush resource shape, D3D12 FillRect rasterizes the first linear-gradient material through clamp-free per-corner SDR vertex-color interpolation with bounded segmented fallback for clamp-crossing fills, D3D12 text and legacy window output still downgrade unsupported non-solid material paths through deterministic SDR fallback, material output diagnostics/backend capability visibility and linear-gradient rasterization-shape counters are implemented for the active D3D12 SDR path, public material authoring remains guard-deferred from style/property APIs and has a documented policy preflight, the non-solid material policy preflight is documented and guard-covered, and the HDR output mapping preflight is documented and guard-covered. The remaining color slices are richer gradient/image material rasterization, public material authoring implementation, and HDR backend/compositor output mapping implementation. Leave HDR/scRGB/Rec.2100 support, per-monitor color mapping, system SDR brightness, and tone mapping to that dedicated slice. |
| P1 | Main-app composition integration | Main app wheel input starts compositor scroll presentation ticks; hover/press/release can route through active presentation; chained retarget distance, compositor-thread sample-and-cancel continuity, `EnsureRunning` rapid wheel coalescing, top/bottom boundary wheel clamping, resize/DPI/max-scroll lifecycle cancellation, lifecycle idle waiter release, invalidation-after-explicit-cancel non-double-counting, and stale queued tick suppression are diagnostic/test covered. Continue hardening cancellation, retarget, hit-test, interaction, and marker behavior without adding a generic fallback compositor. |
| P1 | Framework ownership contracts | `D3D12DrawingBackend` and translator core promotion are complete. `WindowBackend` stays in Poc. Scroll/input ownership is documented in `Poc-Promotion-Contracts.md`; do not move runtime types until a separate extraction commit introduces the required adapters and removes Counter-specific message/action assumptions. |
| P1 | Allocation follow-up hold | Keep the current `--diagnose-text-cache 180` baseline as a future comparison point. Do not resume retained array, snapshot, or tree-builder optimization without an ownership design and a single measured target bucket. |
| P1 | Glyph atlas expansion / maintenance | Add coverage aggressively when it is target-architecture work and includes matching oracle/regression/degradation checks; reject unguarded coverage drift. |
| P2 | Entry-level eviction | Keep design-only until retained atlas command ownership and sub-rect free-list ownership are explicit. |
| P2 | Pixel/layout oracle | Separate future work; do not block the current D3D12-only renderer foundation. |
