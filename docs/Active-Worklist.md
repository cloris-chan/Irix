# Active Worklist

> Current private work items. This file is not a public delivery plan, compatibility contract, or product schedule.

---

## Windows Boundary

The Windows PoC separates target framework, CI toolchain check, and runtime minimum. Windows-targeted projects inherit `IrixWindowsTargetFramework=net10.0-windows10.0.26100.0` and `IrixWindowsSupportedOSPlatformVersion=10.0.15063.0` from `Directory.Build.props`; local builds rely on the .NET SDK to resolve a compatible installed Windows SDK such as 26100 or 28000 and report missing SDKs. The target framework stays on 26100 because .NET SDK 10.0.300 does not yet provide `Microsoft.Windows.SDK.NET.Ref` / TFM support for 28000, and CI verifies that Windows SDK 26100 or newer is installed instead of requiring one exact SDK directory. The 10.0.15063.0 runtime floor is intentional for PerMonitorV2 DPI awareness and display scale support. `IDWriteFactory4` is available within this runtime target and is the baseline DirectWrite factory for the glyph atlas path.

---

## Completed Context

- D3D12 GlyphAtlas text composition is the active Windows text path. DirectWrite/WIC remain source-data providers only.
- `D3D12DrawingBackend` and helper structs live in `Irix.Platform.Windows`.
- `TranslatorCore`, `TranslatorInput`, `TranslatorOutput`, and `TranslatorRetainedState` live in `Irix.Rendering`; `WindowDrawCommandTranslator` remains Poc glue.
- Glyph entries and atlas pages use stable value handles with generations; page reuse is format-scoped and retained-floor-gated.
- Text-cache allocation attribution exists as optional diagnostics. Further tree/layout/snapshot allocation work is paused until an ownership design names one target bucket.
- Retained partial apply and segmented render-source handoff exist behind explicit ownership/freshness/range guards and are enabled by default in the Poc runtime; `--no-partial-apply` remains the rollback switch. This is post-publication command/resource/hit-target reuse, not a `RenderPipeline.Build` layout skip.
- The color pipeline direction is written in [Color-Pipeline.md](Color-Pipeline.md): internal `Color` is now a 16-byte ideal linear BT.2020 / Rec.2020 straight-alpha value for style/property color and draw-command/material payloads, while the current active renderer remains SDR/sRGB and only converts to compact `DrawColor` / `WindowColor` / D3D12 R8G8B8A8 payloads at explicit output boundaries. Internal solid-color and linear-gradient material payloads exist for retained/layer-cache use, D3D12 FillRect rasterizes clamp-free linear gradients through per-corner SDR vertex-color interpolation and uses bounded segmented fallback when clamp boundaries cross the fill, material output diagnostics now distinguish single-rect versus segmented linear-gradient rasterization, D3D12 text and legacy window output still use deterministic SDR fallback for unsupported non-solid material paths, public material authoring remains guard-deferred but now has a policy preflight requiring semantic paint intent rather than renderer-owned material/resource handles, and HDR/scRGB/Rec.2100 output is deferred behind the guarded HDR output mapping preflight.
- Style, animation, and GPU composition contracts exist. Style remains internal IR rather than public UI authoring: the first internal semantic declaration layer maps width, height, background, foreground, opacity, translation, and control-state terms one-way into `VirtualNodeProperty` while keeping renderer processing names internal. Compact typed visual color properties can override draw-command payloads and stay visual-only; opacity/translation metadata has a separate composition mask, explicit style-delta work flags, and a preflight compiler for pure transform/opacity declarations. Public style/transition authoring preflight is documented and guard-covered: future UI authoring stays semantic, renderer processing names stay internal, and runtime owns transition start/cancel/retarget/commit before the compiler or diagnostics can participate. The Poc-owned style transition runtime path exists: runtime decisions flow through `StyleTransitionRuntimeCoordinator`, retained snapshots gate compositor install, start/retarget compile to transform/opacity declarations, and cancel/commit clear transform/opacity presentation. The first Counter app/control integration and lifecycle preflight slice also exists: `CounterStyleTransitionBridge` maps a single button `OwnershipSnapshot` delta through semantic transform/opacity declarations, single-target deltas still use the narrow single-decision route while completion tracking resolves the Counter control-state owner key, multi-target button control-state deltas build a Poc-owned `StyleTransitionDecisionBatch`, and `CounterStyleTransitionRuntimeBridge` waits for app render publication before coordinator install or batch activation. The bridge filters control-state entries by actual transform/opacity delta, so focus-only changes stay on the normal visual render path and do not inflate hover-to-press input into a batch. The main-app input transition path now retimestamps after retained publication, anchors a small visible initial sample, and renders one best-effort compositor tick immediately after single or presentation-set activation so short button-press transitions are not lost before the pump's next scheduled tick. Active-scroll fallback still uses normal dispatch with explicit style transition presentation/tracker cleanup while the scroll schedule is active, but retained last-presented scroll samples after idle no longer block the first post-scroll button press transition. Blocked batch activation clears stale presentation state after app state publication. `StyleTransitionCompletionTracker` is now a Poc-owned marker-driven completion tracker with an owner table keyed by `StyleTransitionOwnerKey`; the table supersedes an older active owner for the same target when a new control-state owner takes over, so focused-to-pressed cross-button transitions do not leave stale single-target owners alive. `StyleTransitionCompletionPump` connects that tracker to the current Counter main-app path: `Once` transform/opacity transitions are decorated with a completion marker, compositor ticks publish that marker, the pump drains it, and the runtime coordinator applies the explicit `Commit` decision without adding a public or generic scheduler. Pump lifecycle diagnostics now make no-active, tick-rendered, completion-committed, tick-unavailable, error states, and active owner count observable through an internal Poc-owned snapshot/formatter. The pump also has an active presentation-set completion path that renders presentation-set ticks, drains per-layer completion markers, commits completed owner entries, clears their active presentation targets, preserves remaining owners, and drops the active presentation plan after the last tracked owner completes; a running pump re-samples the active compositor plan before each tick so an in-flight single-target transition can switch to a freshly activated presentation set. The true concurrent multi-owner transition preflight now includes Poc-owned batch/owner value types, deterministic acceptance/rejection tests, coordinator `ValidateBatch` coverage, validation-only `ValidateBatchRuntime` requirements pairing, rendering-owned presentation-set validation/conflict reporting for duplicate layer ids and overlapping command ranges, rendering-owned activation preflight/plan-set data, active presentation-set compositor state/tick contract, Poc-owned all-ready start/retarget activation bridge, Poc-owned all-ready cancel/commit clear bridge, active presentation-set completion pumping, and owner-table completion tracking. The activation bridge rejects mixed/blocked batches, missing retained snapshots or frames, no presentation owner, and unsupported cancel/commit batches before presentation changes, then attaches completion markers and records owner tracking only after compositor acceptance. The clear bridge rejects mixed/blocked batches, start/retarget owners, missing retained snapshots, and missing tracked owners before mutation, then clears only matching active presentation targets, keeps remaining plan entries alive, and updates only matching owner-table entries. Counter multi-target transition routing now uses the existing concurrent-owner preflight for all-ready button control-state batches. The active composition implementation direction is D3D12-backed transform/opacity, fixed-clip scroll presentation, retained target resolution, marker event publication, multi-layer composition execution, active presentation-set transform/opacity ticks, active hit-test remapping, main-app scroll presentation production, compositor-thread scroll retarget sample-and-cancel, reason-typed lifecycle invalidation/cancellation, and D3D12 layer content caching. Recent hardening covers fixed-clip/nested/sibling-layer hit-test remapping, mapped/unmapped marker runtime dispatch including cross-batch pump accounting, empty sample-and-cancel without cancellation, superseded/sample-and-cancel scroll-presentation idle waiters, explicit/render-invalidation/dispose idle waiter release, invalidation-after-explicit-cancel non-double-counting, lifecycle stale queued tick suppression, scroll marker cancellation queue cleanup, completed scroll presentation active-schedule versus retained-sample separation, non-finite/zero pending pixel suppression across scroll presentation and the legacy scroll frame pump, zero-pixel wheel dispatch suppression, invalid max-scroll feedback suppression/normalization, ScrollController numeric state/delta/tick/presented/max-scroll recovery, composition scalar animation/tick output recovery, composition timestamp arithmetic/timeline overflow recovery, composition layer transform/opacity/fixed-clip value recovery, and fixed-clip/interleaved-paint-order layer-cache behavior.
- A guard-scoped diagnostics scenario transcript fixture exists for scroll presentation continuity, style transition completion, and material output diagnostics. It reuses existing snapshots and formatters without adding CLI flags, tracked golden files, public diagnostics API, or a unified diagnostics channel; expand it only when a concrete regression needs scenario-level coverage.
- The scroll/input runtime owner boundary is written in `Poc-Promotion-Contracts.md`: logical scroll and input ownership belong to app/control runtime; layout feedback, compositor sampling, hit-test service access, input action mapping, and app message dispatch each require explicit adapter contracts before code moves. The first Poc-owned feedback sink, app dispatch intent shape, wheel-input dispatch intent shape, max-scroll feedback dispatch mapper, scroll-presentation interrupt mapper path, hit-test service, input action mapper, input-route app-message dispatch mapper, wheel-input scroll presentation dispatch sink, app runtime dispatch sink, and marker-runtime dispatch-through-sink cuts exist. Scroll presentation coordinator coverage now includes counteracting pending-pixel coalescing before sampling and active-loop reverse-input retargeting from the latest compositor sample. The intent shapes only group Poc runtime dispatch reasons and wheel raw-to-pixel dispatch before `CounterMessage` mapping or scroll presentation delivery; they are not public contracts, diagnostics channels, event buses, or extraction licenses. Marker runtime dispatch source guards now pin the app-owned mapper/sink boundary without extracting Poc runtime.
- Viewport-space internal offscreen caching was removed from the mainline. Secondary paths are reserved for written blockers.

---

## Current Work

### Local Gates

| Task | Current state | Acceptance |
|------|---------------|------------|
| Local validation entrypoint | `.\scripts\validate.ps1` is the fixed local runner while GitHub Actions quota is exhausted. `Quick` mirrors CI quick validation and skips the heavy `Guard` category, `Focused` validates high-signal source/architecture `Guard` checks while skipping lower-frequency `DocGuard` wording and source-shape audits across the category and name-filter lanes. `GlyphSmoke` delegates the guarded glyph smoke lane, and `Full` runs the Release test suite with optional glyph smoke. | `validation.guard status=Passed`; failed lanes report non-zero exit codes and the lane name. |
| Local Smoke gate | GitHub Actions quota is exhausted, so local `.\scripts\glyph-atlas-regression.ps1 -Mode Smoke` remains the broad rendering/glyph gate. | Guard `status=Passed`; matrix `degradedRuns=0`, `glyphAtlasInitialized=True`, `finalComposition=D3D12`; soak `deviceLost=False`, `syncWaits=0`, `hardFullWithoutReuse=0`, `RecordFailed=0`. |
| Guarded glyph coverage | Glyph atlas script/format support is guard-gated by oracle/regression/degradation coverage. | Add new coverage only when it has matching oracle/regression coverage; reject unguarded coverage drift. |
| Source boundary guards | Active guards keep final text composition on D3D12 GlyphAtlas, block runtime shader compile, block raw retained text strings, and prevent premature compositor/runtime extraction. | Guards stay green after renderer, text, diagnostics, or architecture-boundary changes. |

Run `Quick` for routine changes and `Focused` after high-signal source/architecture guard, diagnostics, partial-apply, composition, or scroll/input edits. Run `Full` when lower-frequency `DocGuard` wording or source-shape audits matter. Run `Smoke` before/after broad rendering changes. Do not add artifact-upload work until Actions quota returns.
Push/PR CI intentionally stays lightweight while quota is constrained; run the manual `glyph-atlas-smoke` workflow_dispatch suite only when remote confirmation is worth the cost.

### Near-Term Plan

| Order | Work | Acceptance |
|-------|------|------------|
| 1 | Scroll presentation continuity | Preserve the compositor-thread sample-and-cancel retarget invariant: first retarget without active presentation must return an empty sample without recording cancellation, same-target boundary input must not cancel/restart, replacement segments must start from the latest compositor-presented value, completed presentations may retain the last sample without counting as active input-transition blockers, counteracting pending pixels must drain without sample/cancel, active-loop reverse input must retarget from the latest compositor sample, sample-and-cancel and lifecycle cancellation must complete existing idle waiters, and stale delayed ticks from canceled or replaced segments must not queue after the schedule changes. Keep the sample/cancel, active-schedule, stale delayed tick skip, idle waiter, boundary, counteracting-pending, and reverse-input coordinator tests green. |
| 2 | Main-app lifecycle matrix | Extend only narrow coverage around concrete bugs in resize, DPI, max-scroll feedback, render invalidation, queued compositor ticks, marker publication, and active hit-test routing. Render invalidation stale delayed tick cancellation, lifecycle idle waiter release, and invalidation-after-explicit-cancel non-double-counting have focused compositor-loop coverage; lifecycle scenarios now expose stale queued tick suppression without advancing composition/loop tick counts. Keep lifecycle scenarios diagnostic-visible with stable presented-origin continuity and no generic fallback compositor. |
| 3 | Scroll/input ownership contract | Boundary is written in [Poc-Promotion-Contracts.md](Poc-Promotion-Contracts.md). The first Poc-owned scroll presentation adapter cut exists for runtime dispatch, compositor sampling/start, retained-snapshot access, control feedback delivery, app dispatch intent grouping, wheel-input dispatch intent grouping, max-scroll feedback message mapping, scroll-presentation interrupt mapping, wheel-input scroll presentation dispatch, app runtime dispatch, and marker-runtime dispatch through that sink; the input route now consumes Poc-owned hit-test service, input action mapper, and app-message dispatch mapper shapes for resolved action activation. Marker runtime dispatch source guards pin that the Counter mapper, runtime event ids, and app runtime dispatch sink stay in Poc. Next code moves must return to concrete scroll-presentation continuity and lifecycle bugs rather than moving Counter/Poc runtime types wholesale. |
| 4 | D3D12 direct composition hardening | Keep transform/opacity, fixed-clip scroll, nested/mixed-clip decomposition, marker dispatch, layer content cache, and skip diagnostics on the D3D12-first path. Recent boundary coverage pins fixed-clip/nested/sibling-layer hit-test remapping, mapped/unmapped marker runtime dispatch with cross-batch pump accounting, scroll marker cancellation queue cleanup, fixed-clip cache-hit state, and layer-cache paint order with interleaved ordinary commands. Add more tests only for concrete behavior boundaries, especially marker/skip interactions or main-app integration regressions. |
| 5 | Style transition runtime hardening | Internal semantic declarations now feed the existing style IR and Poc control-state/transition adapters. Public style/transition authoring, Poc runtime ownership, first Counter app/control transition integration, lifecycle preflight, the marker-driven completion tracker, the main-app completion pump, active-scroll fallback cleanup, routed Counter multi-target transition policy, completion-pump lifecycle diagnostics, true concurrent multi-owner transition design preflight, Poc-owned batch/owner value types with deterministic acceptance/rejection tests, coordinator batch validation against one retained snapshot, validation-only batch runtime preflight, compositor-side presentation-set validation/conflict reporting without install, rendering-owned activation preflight/plan-set data, active presentation-set compositor state/tick contract, Poc-owned all-ready start/retarget activation bridge, Poc-owned all-ready cancel/commit clear bridge, active presentation-set completion pumping, owner-table completion tracking with same-target owner supersession, per-owner active presentation clear, Counter multi-target routing, running-pump single-to-presentation tick-mode switching, routed multi-target completion tick-mode diagnostics, and blocked fallback batch activation diagnostics over the existing preflight are implemented. Keep generic scheduling and public authoring names deferred; expand only around concrete main-app regressions. |
| 6 | Color material/output payload migration | Draw commands now carry canonical `Color` internally, the current SDR/sRGB output stage is named by an internal `ColorOutputMapping.SdrSrgb` boundary used by D3D12 and legacy window output, internal solid-color and first linear-gradient `DrawMaterial`/brush resource shapes exist for layer-cache payloads, D3D12 FillRect rasterizes the first linear-gradient material through clamp-free per-corner SDR vertex-color interpolation with bounded segmented fallback for clamp-crossing fills, D3D12 text and legacy window output still use deterministic SDR fallback for unsupported non-solid material paths, the active D3D12 SDR path reports selected material kind/backend capability/fallback reason/fallback counts plus single-rect versus segmented rasterization counters through material output diagnostics, public material authoring is guard-deferred from style/property APIs and has a policy preflight requiring semantic paint intent rather than renderer-owned material/resource handles, the non-solid material policy preflight names the required lifetime, coordinate, interpolation, invalidation, backend fallback, and diagnostics contracts, and the HDR output mapping preflight names the output context, screen-region, SDR white/tone mapping, swapchain capability, payload format, diagnostics, and fallback contracts. Remaining color work is richer gradient/image material implementation, public material authoring implementation, and HDR output mapping implementation. Do not implement HDR swapchains, Rec.2100 HLG/PQ, per-monitor color mapping, system SDR brightness, or tone mapping in this slice. |
| 7 | Scenario diagnostics fixture discipline | Keep the scenario transcript fixture guard-scoped and formatter-backed. Add new scenario transcript lines only for concrete regressions; do not turn it into a CLI surface, golden-file suite, public diagnostics API, or unified diagnostics channel. |
| 8 | Glyph and allocation discipline | Run glyph Smoke for broad rendering changes. Keep allocation optimization paused unless a written ownership design names one measured bucket and proves retained publication safety. |

### Architecture And Composition

| Task | Current state | Acceptance |
|------|---------------|------------|
| Style system | [Style-System.md](Style-System.md) defines internal layout, visual, text shaping, composition, and control-state classification. `StyleDeclaration` now provides an internal semantic declaration bridge for width, height, background, foreground, opacity, translation, and control-state terms before mapping into `VirtualNodeProperty`; compact typed internal visual color properties feed draw-command color payloads, composite metadata has its own mask, and pure opacity/translation deltas can precompile to existing transform/opacity declarations. Public authoring preflight defines semantic future terms and forbidden internal processing names; the Poc runtime coordinator, Counter transition bridge, `StyleTransitionCompletionTracker`, and `StyleTransitionCompletionPump` prove the first app/control decision, lifecycle preflight, marker-driven completion boundary, main-app completion path, completion-pump diagnostics, active-scroll fallback cleanup, and routed Counter multi-target transitions over the concurrent-owner preflight. | Keep public UI style semantic; implement against internal metadata and invalidation rules without adding generic compatibility style layers, public processing-layer authoring names, theme/cascade, or a generic scheduler before lifecycle ownership is explicit. |
| Color pipeline | [Color-Pipeline.md](Color-Pipeline.md) defines the canonical color value as ideal linear BT.2020 / Rec.2020 straight-alpha. Internal `Color`, `SrgbColor`, `StyleColor`, `PropertyValue.Color`, the internal `DrawCommand` color/material payload, the internal solid-color and linear-gradient brush resource shapes, D3D12 FillRect linear-gradient per-corner SDR rasterization with bounded clamp fallback and rasterization-shape diagnostics, material output diagnostics/backend capability visibility, the public material-authoring deferral guard and policy preflight, the non-solid material policy preflight, the HDR output mapping preflight, and the current `ColorOutputMapping.SdrSrgb` mapper cover the first SDR/sRGB slice; `DrawColor` and `WindowColor` remain compact SDR bridge/output storage. | Next color code slice can start richer gradient/image material rasterization preflight/implementation, public material authoring implementation, or HDR output mapping implementation behind the preflight. Public material authoring implementation and HDR/scRGB/Rec.2100 output still wait for dedicated slices. |
| Animation/composition | [Animation-Composition.md](Animation-Composition.md) defines UI-runtime, compositor, hybrid, and backend-internal animation classes. Runtime transform/opacity and fixed-clip scroll declarations resolve retained `NodeKey` targets into compositor ticks. Public transition authoring preflight now pins that future authoring describes semantic properties and timing while runtime owns start/cancel/retarget/commit and the compiler remains pure; the Poc-owned coordinator/adapters now enforce that ownership before compositor install, `StyleTransitionCompletionTracker` uses compositor marker events only to create explicit runtime commit decisions for `Once` transform/opacity transitions and now stores active entries in an owner table with same-target owner supersession, and `StyleTransitionCompletionPump` drains those events on the Counter main-app path with lifecycle diagnostics for no-active, tick-rendered, completion-committed, tick-unavailable, error states, active owner count, and running single-to-presentation tick-mode switching. Concurrent transition owner preflight now has runtime-owned owner identity, decision batches, coordinator validation against one retained publication, validation-only runtime requirements pairing, compositor-side presentation-set validation/conflict reporting without install, rendering-owned activation preflight/plan-set data, active presentation-set compositor state/tick contract, Poc-owned all-ready start/retarget activation bridge, Poc-owned all-ready cancel/commit clear bridge, per-owner active presentation clear, active presentation-set completion pumping, and owner-table completion tracking; Counter multi-target transition routing now uses the existing concurrent-owner preflight for all-ready button control-state batches. Marker dispatch, scroll interruption policy, live wheel retarget with target probing plus compositor-thread sample-and-cancel, main-app scroll presentation, reason-typed invalidation cancellation, superseded/sample-and-cancel/lifecycle idle waiter completion, scroll runtime cancellation matrix diagnostics, active scroll hit-test/press diagnostics, main-app scroll interaction diagnostics including `EnsureRunning` rapid wheel coalescing, resize/DPI/viewport/tree/layout/text/max-scroll lifecycle cancellation, and stale queued tick suppression, plus skipped-compositor diagnostics exist. | Keep broad app integration on the D3D12-first path; harden cancellation/retarget/interaction semantics when bugs appear. |
| GPU composition architecture | [GPU-Composition-Architecture.md](GPU-Composition-Architecture.md) defines IR, capabilities, GPU offload order, and backend mapping notes. D3D12 transform/opacity, fixed-clip scroll, runtime marker dispatch, marker queue cleanup on scroll cancellation, multi-layer frame execution, nested/mixed-clip decomposition, main-app scroll presentation, reason-typed scroll lifecycle cancellation, layer content caching, and skip diagnostics exist. | Continue D3D12-first. Do not start Vulkan/Metal before the active backend proves those contracts. |
| Scroll compositor | Fixed-clip scroll presentation is implemented for single-layer and decomposed nested/mixed-clip retained targets. Logical scroll remains app/control runtime; presented scroll is compositor-owned, and retarget sample/cancel, cancellation reasons, plus main-app resize/DPI/viewport/tree/layout/text/max-scroll cancellation paths are diagnostic-visible. | Continue hardening interruption, lifecycle cancellation, and hit testing without a generic fallback compositor. |
| D3D12 composition execution | [D3D12-Composition.md](D3D12-Composition.md) covers animation declarations/plans, scroll presentation declarations/plans, compositor-owned ticks, hit-test remapping, marker events, runtime marker dispatch, marker queue cleanup on canceled scroll presentation, multi-layer frame execution, retained nested/mixed-clip decomposition, composition layer transform/opacity/fixed-clip value normalization, D3D12 layer content cache diagnostics including display-scale, resource-resolver, resource-frame reset invalidation, disjoint multi-layer hits, overlapping-layer direct fallback, fixed-clip cache-hit state, interleaved source paint-order preservation, backend capabilities, skip diagnostics, typed scroll runtime cancellation diagnostics, and visible diagnostic commands. | Keep ticks independent of UI rebuild; expand direct-path coverage only when a concrete behavior needs it. |
| D3D12-first GPU offload | Active path is direct/layer-content D3D12 composition. Internal offscreen/render-target caching is not active because viewport-space caching broke fixed-clip scroll semantics. | Consider offscreen only after content-space bounds, origin, clip, invalidation, and hit-test semantics are designed and direct composition still needs it. |

### Framework Boundary

| Task | Current state | Acceptance |
|------|---------------|------------|
| Translator boundary | Core translator types live in `Irix.Rendering`. `WindowDrawCommandTranslator` remains in `Irix.Poc` because it composes viewport callbacks, app/control scroll feedback, diagnostics, optional allocation attribution, and Counter default style construction. Source guards now prevent Counter/Poc runtime identity and dispatch adapters from leaking into `Irix.Rendering` or `Irix.Platform.Windows`. | Do not move the outer adapter or make the core public without a new contract. |
| Windows drawing backend | `D3D12DrawingBackend` and helper structs live in `Irix.Platform.Windows`. | Preserve scissor/text clip/device recovery/scale diagnostics tests; do not move it back into `Irix.Poc`. |
| WindowBackend isolation | `WindowBackend` is a legacy/debug `INativeWindow.SetContent` presentation path. | Do not move it into `Irix.Rendering` or `Irix.Platform.Windows`; replace only if the legacy/debug path becomes unnecessary. |
| Scroll ownership | `ScrollDiagnostics` is layout observation; `ScrollFeedback` is app/control feedback; `ScrollController`, `ScrollState`, `ScrollFramePump`, and `ScrollPresentationCoordinator` remain app runtime state in `Irix.Poc`. The scroll/input runtime owner boundary is written, and `ScrollPresentationCoordinator` now consumes Poc-owned runtime, compositor, and retained-snapshot adapter shapes instead of directly depending on the compositor loop and translator inside its core retarget path. `WindowDrawCommandTranslator` now delivers projected feedback through a Poc-owned `IControlFeedbackSink`. | Do not move scroll runtime code until a separate extraction commit introduces broader app-message dispatch and framework runtime owner contracts. |
| Settings provider | Runtime settings wiring remains postponed. | Add only after a concrete app/framework boundary is written; keep the internal default provider until then. |
| Input/control projection | `InputOwnershipState`, `ControlVisualState*`, and `ActionHitTestResolver` remain Counter/Poc runtime projection. `InputOwnershipState` now consumes a Poc-owned `IInputHitTestService`, and `CounterInputRouter` has Poc-owned input action, dispatch intent, and app-message dispatch mapper boundaries for resolved `ActionId` activation. The owner boundary still requires broader dispatch coverage before extraction. | Do not promote input/control code without replacing Counter-specific action identity, `CounterMessage` mapping, and single-pointer assumptions. |

### Allocation Hold

| Task | Current state | Acceptance |
|------|---------------|------------|
| Allocation attribution baseline | Latest `--diagnose-text-cache 180` warm scroll sample is the comparison point: about `2204 B/frame`; `tree.buildRoot=546` B/frame, `pipeline.layout=501` B/frame, `layout.nodeWalk=0`, record `45 B/frame`. Safe empty publication array reuse is the only optimization from that work. | Reopen aggressively only with an ownership design and one measured target bucket. |
| Layout builder scratch ownership | Layout full/dirty allocation is stable but still visible in attribution. | Design first. Scratch lifetime and pooling must not leak retained state, stack memory, or rented arrays. |
| Retained snapshot boundary review | Snapshot copy allocation is visible but not dominant. | Design first. Any reuse design preserves `TextSlice`/resource snapshot validity and cross-frame ownership rules. |

Layout publication note: main layout arrays are retained publication state, not disposable same-frame scratch. `Elements`, `TreeNodes`, `DirtyElementRanges`, and `ScrollDiagnostics` are retained by `LastLayoutResult` / `LastRetainedInputSnapshot`. Safe reuse is limited to empty/static collections or same-frame scratch that is copied before publication.

### Glyph Atlas Follow-Up

| Task | Current state | Acceptance |
|------|---------------|------------|
| Local/manual soak cadence | `Smoke` is routine; `Local` after glyph/page/shaping changes; `Nightly` after page-policy, eviction, or shaping overhauls. | Evidence is updated only when results materially change or cover a representative change. |
| BGRA/TIFF natural font coverage | Default local Segoe UI Emoji naturally covers COLR/layer; Noto probes naturally cover PNG image data. Direct BGRA/TIFF natural font coverage remains unavailable locally. | Add evidence if a real font/environment exposes direct BGRA or TIFF; otherwise keep code-level selector/raster/decode/page-format tests. |
| Entry-level eviction design | Design exists; implementation is not selected until ownership is explicit. | Implement entry LRU/sub-rect free-list only after retained atlas command ownership exposes an oldest retained atlas record serial and page-local free-list ownership is explicit. |
| Pixel/layout oracle | Structural DirectWrite analyzer/glyph oracles exist; pixel/layout oracle is deferred. | Future work remains separate from current regression matrix and must validate the D3D12 atlas path. |

### Deferred Architecture

| Task | Current state | Acceptance |
|------|---------------|------------|
| Unified diagnostics channel | Postponed. | Replace per-component diagnostics only after current snapshots prove insufficient. |
| StyleOnly layout skip | `RenderPipeline.Build` layout skip remains unimplemented. Segmented retained-frame partial apply and selected render-source handoff are implemented and Poc-default-on, but they consume the current retained publication after normal layout/draw work; they are not a `LayoutTreeBuilder` skip. A focused preflight guard now pins the full-layout-rebuild boundary for StyleOnly while proving retained partial apply can still accept post-publication reuse. | Implement only as a separate pipeline fast path that proves retained layout reuse, current-frame resources, hit target metadata, dirty ranges, and fallback behavior. |
| Second graphics backend | Deferred. | Start only after D3D12 composition contracts and project/layer boundaries are stable. |

---

## Explicit Non-Goals

- Final text composition stays on the D3D12 GlyphAtlas path.
- No unguarded glyph script/format coverage expansion; new coverage needs matching oracle/regression coverage.
- No entry LRU or sub-rect free-list implementation before the ownership contract exists.
- No pixel oracle or DirectWrite layout renderer as a blocker for current D3D12 work.
- No retained-array, snapshot, or tree-builder allocation optimization without an ownership design plus attribution.
- No compatibility-driven public API expansion or code migration from `Irix.Poc` without a written contract.
- No compositor/runtime code extraction before the style, animation, and composition contracts are ready.
- No generic CPU/compatibility compositor as the first implementation route; use existing normal frame rendering as the explicit secondary path only when the D3D12-first path hits a written blocker.
- No Vulkan/Metal backend implementation in the current worklist.
- No HDR color output implementation in the current worklist; keep the active renderer on SDR/sRGB until the HDR backend/compositor output mapping slice is selected.
